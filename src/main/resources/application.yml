# Spring Boot 主配置文件
# ============================================================================

spring:
  application:
    name: SpringWeb
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}  # 默认开发环境

  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        # 🔧 PostgreSQL JSONB配置
        jdbc:
          batch_versioned_data: true

  # 🔧 Jackson JSON配置
  jackson:
    serialization:
      fail-on-empty-beans: false
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      enabled: true

  web:
    resources:
      static-locations: classpath:/static/,file:D:/Code/Learn/Java/Spring/image/uploads/

# 基础配置（所有环境共享）
server:
  port: 8080
  servlet:
    context-path: /api

# 日志配置
logging:
  level:
    com.cmliy.springweb: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    # 🔧 日志配置（调试用）
    com.cmliy.springweb.service.ProductDataService: DEBUG
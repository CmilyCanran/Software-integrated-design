---
tags:
  - 高级查询
  - JPQL
  - 原生SQL
  - 动态查询
  - Specification
created: 2025-11-19
modified: 2025-11-19
category: 数据库技术
difficulty: intermediate
---

# 04-高级查询技术

> **学习目标**: 掌握复杂查询技术，实现灵活的数据检索功能

## 🎯 本章概览

**学习时间**: 60-90分钟 | **难度等级**: ⭐⭐⭐⭐ | **重点程度**: 🔥🔥🔥🔥🔥

当简单的查询方法无法满足需求时，我们需要使用高级查询技术。本章将教你JPQL、原生SQL、动态查询等技术，让你能够处理复杂的业务查询需求。

---

## 📋 核心需求

### 🎯 业务目标
- 实现复杂的条件查询
- 支持动态查询条件组合
- 提供数据统计和聚合功能
- 优化复杂查询性能

### 🛠️ 技术需求
- JPQL查询语法
- 原生SQL查询
- Criteria API动态查询
- Specification模式
- 查询结果投影

---

## 🏗️ 高级查询技术体系

### 🎯 查询技术选择指南

```mermaid
graph TD
    A[查询需求] --> B{查询复杂度}
    B -->|简单查询| C[方法名查询]
    B -->|中等复杂| D[@Query JPQL]
    B -->|非常复杂| E[原生SQL]
    B -->|动态条件| F[Specification]

    C --> G[Repository实现]
    D --> G
    E --> G
    F --> G

    style A fill:#e3f2fd
    style C fill:#c8e6c9
    style D fill:#fff9c4
    style E fill:#ffccbc
    style F fill:#f8bbd9
    style G fill:#e1bee7
```

---

## 💻 实战：JPQL查询技术

### 1️⃣ 基础JPQL查询

```java
@Repository
public interface UserRepository extends JpaRepository<User, Long>, JpaSpecificationExecutor<User> {

    // 🔍 基础JPQL查询 - 根据角色和创建时间查询用户
    @Query("SELECT u FROM User u WHERE u.role = :role AND u.createdAt >= :startDate")
    List<User> findUsersByRoleAndCreatedAfter(
            @Param("role") String role,
            @Param("startDate") LocalDateTime startDate);

    // 📊 聚合查询 - 统计各角色用户数量
    @Query("SELECT u.role, COUNT(u) FROM User u GROUP BY u.role")
    List<Object[]> countUsersByRole();

    // 🔍 复杂条件查询 - 活跃用户（最近有登录）
    @Query("SELECT u FROM User u WHERE " +
           "u.lastLoginTime IS NOT NULL AND " +
           "u.lastLoginTime >= :since AND " +
           "u.role != 'GUEST'")
    List<User> findActiveUsers(@Param("since") LocalDateTime since);

    // 📊 分页查询 - 按角色分页并排序
    @Query("SELECT u FROM User u WHERE u.role = :role ORDER BY u.createdAt DESC")
    Page<User> findUsersByRoleOrderByCreatedAtDesc(
            @Param("role") String role,
            Pageable pageable);

    // 🎯 投影查询 - 只返回需要的字段
    @Query("SELECT u.id, u.username, u.email, u.createdAt FROM User u WHERE u.role = :role")
    List<Object[]> findUserSummariesByRole(@Param("role") String role);
}
```

### 2️⃣ 商品复杂查询

```java
@Repository
public interface ProductRepository extends JpaRepository<Product, Long>, JpaSpecificationExecutor<Product> {

    // 🔍 多条件查询 - 商品筛选
    @Query("SELECT p FROM Product p WHERE " +
           "(:category IS NULL OR p.category = :category) AND " +
           "(:minPrice IS NULL OR p.price >= :minPrice) AND " +
           "(:maxPrice IS NULL OR p.price <= :maxPrice) AND " +
           "(:minStock IS NULL OR p.stock >= :minStock)")
    Page<Product> findProductsByFilters(
            @Param("category") String category,
            @Param("minPrice") BigDecimal minPrice,
            @Param("maxPrice") BigDecimal maxPrice,
            @Param("minStock") Integer minStock,
            Pageable pageable);

    // 📊 统计查询 - 分类商品统计
    @Query("SELECT p.category, COUNT(p), AVG(p.price), MIN(p.price), MAX(p.price) " +
           "FROM Product p GROUP BY p.category")
    List<Object[]> getProductStatisticsByCategory();

    // 🔍 关键词搜索 - 全文搜索
    @Query("SELECT p FROM Product p WHERE " +
           "LOWER(p.name) LIKE LOWER(CONCAT('%', :keyword, '%')) OR " +
           "LOWER(p.description) LIKE LOWER(CONCAT('%', :keyword, '%'))")
    Page<Product> searchProducts(@Param("keyword") String keyword, Pageable pageable);

    // 📈 热门商品 - 按销量排序（如果有订单关联）
    @Query("SELECT p FROM Product p WHERE p.stock > 0 ORDER BY p.price ASC")
    List<Product> findAvailableProductsOrderByPrice();

    // 🎯 价格区间统计
    @Query("SELECT " +
           "CASE " +
           "WHEN p.price < 50 THEN '0-50' " +
           "WHEN p.price < 100 THEN '50-100' " +
           "WHEN p.price < 200 THEN '100-200' " +
           "ELSE '200+' END as priceRange, " +
           "COUNT(p) as count " +
           "FROM Product p GROUP BY " +
           "CASE " +
           "WHEN p.price < 50 THEN '0-50' " +
           "WHEN p.price < 100 THEN '50-100' " +
           "WHEN p.price < 200 THEN '100-200' " +
           "ELSE '200+' END")
    List<Object[]> getProductCountByPriceRange();
}
```

---

## 🛠️ Specification动态查询

### 1️⃣ 用户动态查询规范

```java
@Component
public class UserSpecifications {

    // 🔍 根据用户名模糊查询
    public static Specification<User> hasUsername(String username) {
        return (root, query, cb) -> {
            if (username == null || username.trim().isEmpty()) {
                return cb.conjunction();
            }
            return cb.like(cb.lower(root.get("username")),
                          "%" + username.toLowerCase() + "%");
        };
    }

    // 📧 根据邮箱模糊查询
    public static Specification<User> hasEmail(String email) {
        return (root, query, cb) -> {
            if (email == null || email.trim().isEmpty()) {
                return cb.conjunction();
            }
            return cb.like(cb.lower(root.get("email")),
                          "%" + email.toLowerCase() + "%");
        };
    }

    // 👥 根据角色查询
    public static Specification<User> hasRole(String role) {
        return (root, query, cb) -> {
            if (role == null || role.trim().isEmpty()) {
                return cb.conjunction();
            }
            return cb.equal(root.get("role"), role);
        };
    }

    // 🕐 创建时间范围查询
    public static Specification<User> createdAtBetween(LocalDateTime start, LocalDateTime end) {
        return (root, query, cb) -> {
            if (start == null && end == null) {
                return cb.conjunction();
            }
            if (start != null && end != null) {
                return cb.between(root.get("createdAt"), start, end);
            }
            if (start != null) {
                return cb.greaterThanOrEqualTo(root.get("createdAt"), start);
            }
            return cb.lessThanOrEqualTo(root.get("createdAt"), end);
        };
    }

    // 🔍 组合查询 - 用户名或邮箱
    public static Specification<User> hasUsernameOrEmail(String keyword) {
        return (root, query, cb) -> {
            if (keyword == null || keyword.trim().isEmpty()) {
                return cb.conjunction();
            }
            String lowerKeyword = "%" + keyword.toLowerCase() + "%";
            return cb.or(
                cb.like(cb.lower(root.get("username")), lowerKeyword),
                cb.like(cb.lower(root.get("email")), lowerKeyword)
            );
        };
    }
}
```

### 2️⃣ 商品动态查询规范

```java
@Component
public class ProductSpecifications {

    // 📦 根据名称查询
    public static Specification<Product> hasName(String name) {
        return (root, query, cb) -> {
            if (name == null || name.trim().isEmpty()) {
                return cb.conjunction();
            }
            return cb.like(cb.lower(root.get("name")),
                          "%" + name.toLowerCase() + "%");
        };
    }

    // 📂 根据分类查询
    public static Specification<Product> hasCategory(String category) {
        return (root, query, cb) -> {
            if (category == null || category.trim().isEmpty()) {
                return cb.conjunction();
            }
            return cb.equal(root.get("category"), category);
        };
    }

    // 💰 价格区间查询
    public static Specification<Product> priceBetween(BigDecimal minPrice, BigDecimal maxPrice) {
        return (root, query, cb) -> {
            if (minPrice == null && maxPrice == null) {
                return cb.conjunction();
            }
            if (minPrice != null && maxPrice != null) {
                return cb.between(root.get("price"), minPrice, maxPrice);
            }
            if (minPrice != null) {
                return cb.greaterThanOrEqualTo(root.get("price"), minPrice);
            }
            return cb.lessThanOrEqualTo(root.get("price"), maxPrice);
        };
    }

    // 📊 库存查询
    public static Specification<Product> stockGreaterThan(Integer minStock) {
        return (root, query, cb) -> {
            if (minStock == null) {
                return cb.conjunction();
            }
            return cb.greaterThanOrEqualTo(root.get("stock"), minStock);
        };
    }

    // 🔍 关键词搜索（名称或描述）
    public static Specification<Product> hasKeyword(String keyword) {
        return (root, query, cb) -> {
            if (keyword == null || keyword.trim().isEmpty()) {
                return cb.conjunction();
            }
            String lowerKeyword = "%" + keyword.toLowerCase() + "%";
            return cb.or(
                cb.like(cb.lower(root.get("name")), lowerKeyword),
                cb.like(cb.lower(root.get("description")), lowerKeyword)
            );
        };
    }
}
```

---

## 🧪 动态查询服务实现

### 1️⃣ 用户查询服务

```java
@Service
@Transactional(readOnly = true)
public class UserQueryService {

    @Autowired
    private UserRepository userRepository;

    // 🔍 动态条件查询用户
    public Page<User> searchUsers(UserSearchCriteria criteria, Pageable pageable) {
        Specification<User> spec = Specification.where(null);

        // 组合查询条件
        if (criteria.getUsername() != null) {
            spec = spec.and(UserSpecifications.hasUsername(criteria.getUsername()));
        }
        if (criteria.getEmail() != null) {
            spec = spec.and(UserSpecifications.hasEmail(criteria.getEmail()));
        }
        if (criteria.getRole() != null) {
            spec = spec.and(UserSpecifications.hasRole(criteria.getRole()));
        }
        if (criteria.getStartDate() != null || criteria.getEndDate() != null) {
            spec = spec.and(UserSpecifications.createdAtBetween(
                criteria.getStartDate(), criteria.getEndDate()));
        }

        return userRepository.findAll(spec, pageable);
    }

    // 📊 用户角色统计
    public List<RoleStatistics> getUserRoleStatistics() {
        List<Object[]> results = userRepository.countUsersByRole();
        return results.stream()
                .map(result -> new RoleStatistics(
                    (String) result[0],
                    (Long) result[1]
                ))
                .collect(Collectors.toList());
    }

    // 👥 活跃用户查询
    public List<User> getActiveUsers(int days) {
        LocalDateTime since = LocalDateTime.now().minusDays(days);
        return userRepository.findActiveUsers(since);
    }

    // 🎯 用户摘要查询
    public List<UserSummary> getUserSummariesByRole(String role) {
        List<Object[]> results = userRepository.findUserSummariesByRole(role);
        return results.stream()
                .map(result -> new UserSummary(
                    (Long) result[0],
                    (String) result[1],
                    (String) result[2],
                    (LocalDateTime) result[3]
                ))
                .collect(Collectors.toList());
    }
}

// 📋 查询条件封装
@Data
public class UserSearchCriteria {
    private String username;
    private String email;
    private String role;
    private LocalDateTime startDate;
    private LocalDateTime endDate;
}

// 📊 统计结果封装
@Data
@AllArgsConstructor
public class RoleStatistics {
    private String role;
    private Long count;
}

// 📝 用户摘要封装
@Data
@AllArgsConstructor
public class UserSummary {
    private Long id;
    private String username;
    private String email;
    private LocalDateTime createdAt;
}
```

### 2️⃣ 商品查询服务

```java
@Service
@Transactional(readOnly = true)
public class ProductQueryService {

    @Autowired
    private ProductRepository productRepository;

    // 🔍 动态条件查询商品
    public Page<Product> searchProducts(ProductSearchCriteria criteria, Pageable pageable) {
        Specification<Product> spec = Specification.where(null);

        // 组合查询条件
        if (criteria.getName() != null) {
            spec = spec.and(ProductSpecifications.hasName(criteria.getName()));
        }
        if (criteria.getCategory() != null) {
            spec = spec.and(ProductSpecifications.hasCategory(criteria.getCategory()));
        }
        if (criteria.getMinPrice() != null || criteria.getMaxPrice() != null) {
            spec = spec.and(ProductSpecifications.priceBetween(
                criteria.getMinPrice(), criteria.getMaxPrice()));
        }
        if (criteria.getMinStock() != null) {
            spec = spec.and(ProductSpecifications.stockGreaterThan(criteria.getMinStock()));
        }
        if (criteria.getKeyword() != null) {
            spec = spec.and(ProductSpecifications.hasKeyword(criteria.getKeyword()));
        }

        return productRepository.findAll(spec, pageable);
    }

    // 📊 分类统计
    public List<CategoryStatistics> getCategoryStatistics() {
        List<Object[]> results = productRepository.getProductStatisticsByCategory();
        return results.stream()
                .map(result -> new CategoryStatistics(
                    (String) result[0],
                    (Long) result[1],
                    (BigDecimal) result[2],
                    (BigDecimal) result[3],
                    (BigDecimal) result[4]
                ))
                .collect(Collectors.toList());
    }

    // 📈 价格区间统计
    public List<PriceRangeStatistics> getPriceRangeStatistics() {
        List<Object[]> results = productRepository.getProductCountByPriceRange();
        return results.stream()
                .map(result -> new PriceRangeStatistics(
                    (String) result[0],
                    (Long) result[1]
                ))
                .collect(Collectors.toList());
    }

    // 🛍️ 可用商品查询
    public List<Product> getAvailableProducts() {
        return productRepository.findAvailableProductsOrderByPrice();
    }
}

// 📋 商品查询条件
@Data
public class ProductSearchCriteria {
    private String name;
    private String category;
    private BigDecimal minPrice;
    private BigDecimal maxPrice;
    private Integer minStock;
    private String keyword;
}

// 📊 分类统计结果
@Data
@AllArgsConstructor
public class CategoryStatistics {
    private String category;
    private Long count;
    private BigDecimal avgPrice;
    private BigDecimal minPrice;
    private BigDecimal maxPrice;
}

// 📈 价格区间统计
@Data
@AllArgsConstructor
public class PriceRangeStatistics {
    private String priceRange;
    private Long count;
}
```

---

## 🧪 高级查询测试

### 1️⃣ Specification查询测试

```java
@SpringBootTest
public class SpecificationQueryTest {

    @Autowired
    private UserRepository userRepository;

    @Autowired
    private UserQueryService userQueryService;

    @Test
    public void testDynamicUserSearch() {
        // 🎯 创建测试数据
        createTestUsers();

        // 🔍 动态查询条件
        UserSearchCriteria criteria = new UserSearchCriteria();
        criteria.setUsername("test");
        criteria.setRole("USER");

        // 📊 分页查询
        Pageable pageable = PageRequest.of(0, 10);
        Page<User> result = userQueryService.searchUsers(criteria, pageable);

        // ✅ 验证结果
        assertTrue(result.getContent().size() >= 1);
        result.getContent().forEach(user -> {
            assertTrue(user.getUsername().contains("test"));
            assertEquals("USER", user.getRole());
        });

        System.out.println("✅ 动态查询成功，找到 " + result.getTotalElements() + " 个用户");
    }

    @Test
    public void testUserRoleStatistics() {
        // 🎯 创建测试数据
        createTestUsers();

        // 📊 统计查询
        List<RoleStatistics> statistics = userQueryService.getUserRoleStatistics();

        // ✅ 验证结果
        assertFalse(statistics.isEmpty());
        statistics.forEach(stat -> {
            assertNotNull(stat.getRole());
            assertTrue(stat.getCount() >= 0);
        });

        System.out.println("✅ 角色统计成功：");
        statistics.forEach(stat ->
            System.out.println("  " + stat.getRole() + ": " + stat.getCount() + " 人"));
    }

    private void createTestUsers() {
        // 创建测试用户逻辑
        for (int i = 0; i < 5; i++) {
            User user = new User();
            user.setUsername("testuser" + i);
            user.setEmail("test" + i + "@example.com");
            user.setPassword("password123");
            user.setRole(i % 2 == 0 ? "USER" : "ADMIN");
            userRepository.save(user);
        }
    }
}
```

---

## 🚀 性能优化建议

### ✅ 查询优化策略

1. **🎯 精确字段查询**
   ```java
   // ✅ 推荐：只查询需要的字段
   @Query("SELECT u.id, u.username FROM User u WHERE u.role = :role")
   List<Object[]> findUserIdsAndNamesByRole(@Param("role") String role);
   ```

2. **📊 分页查询大数据集**
   ```java
   // ✅ 推荐：使用分页避免内存溢出
   Page<User> users = userRepository.findAll(PageRequest.of(0, 100));
   ```

3. **🔍 索引优化查询**
   ```java
   // ✅ 推荐：为常用查询字段添加索引
   @Table(name = "users", indexes = {
       @Index(name = "idx_username", columnList = "username"),
       @Index(name = "idx_role_created", columnList = "role, created_at")
   })
   ```

---

## 📝 本章小结

### ✅ 已掌握技能

- [ ] **理解** JPQL查询语法
- [ ] **能够** 编写复杂查询
- [ ] **掌握** Specification动态查询
- [ ] **了解** 查询性能优化
- [ ] **能够** 实现统计查询

### 🎯 关键要点

1. **JPQL查询** - 面向对象的查询语言
2. **动态查询** - Specification模式实现灵活查询
3. **统计查询** - 聚合函数和分组查询
4. **性能优化** - 索引和分页优化查询

### 🚀 下一步学习

现在你已经掌握了高级查询技术，接下来可以学习：
- → **05-事务管理** - 学习数据库事务控制
- → **功能一后端开发** - 开始实现用户认证系统
- → **实战项目** - 应用查询技术到实际业务

---

**记住：高级查询是处理复杂业务需求的关键技能！** 🎉

---
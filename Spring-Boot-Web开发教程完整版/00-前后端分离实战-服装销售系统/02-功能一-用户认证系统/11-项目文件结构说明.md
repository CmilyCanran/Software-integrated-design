---
tags:
  - 项目结构
  - 文件位置
  - 代码组织
  - 目录说明
created: 2025-11-19
modified: 2025-11-19
category: 开发教程
difficulty: beginner
---

# 00-项目文件结构说明

> **学习目标**: 了解Spring Boot项目的文件结构和代码组织方式，明确各个文件的放置位置

## 🎯 本章概览

**学习时间**: 15-20分钟 | **难度等级**: ⭐ | **重点程度**: 🔥🔥🔥

本章详细说明Spring Boot项目中各个代码文件的放置位置，帮助你正确组织项目结构。

---

## 📁 完整项目目录结构

```
SpringWeb/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/cmliy/springweb/
│   │   │       ├── SpringWebApplication.java           # 🎯 主启动类
│   │   │       │
│   │   │       ├── config/                             # 📋 配置类目录
│   │   │       │   ├── SecurityConfig.java            # 🛡️ Spring Security配置
│   │   │       │   ├── JwtConfig.java                 # 🔑 JWT配置
│   │   │       │   └── DatabaseConfig.java            # 🗄️ 数据库配置
│   │   │       │
│   │   │       ├── controller/                         # 🌐 控制器层
│   │   │       │   ├── AuthController.java             # 🔐 认证控制器
│   │   │       │   └── UserController.java             # 👤 用户控制器
│   │   │       │
│   │   │       ├── service/                            # 🛠️ 服务层
│   │   │       │   ├── AuthService.java                # 🔐 认证服务接口
│   │   │       │   ├── AuthServiceImpl.java           # 🔐 认证服务实现
│   │   │       │   ├── UserService.java                # 👤 用户服务接口
│   │   │       │   ├── UserServiceImpl.java           # 👤 用户服务实现
│   │   │       │   └── JwtAuthService.java             # 🔑 JWT认证服务
│   │   │       │
│   │   │       ├── repository/                         # 📊 数据访问层
│   │   │       │   ├── UserRepository.java             # 👤 用户仓库
│   │   │       │   └── RoleRepository.java             # 🎭 角色仓库
│   │   │       │
│   │   │       ├── model/                              # 🏗️ 实体模型
│   │   │       │   ├── User.java                       # 👤 用户实体
│   │   │       │   └── Role.java                       # 🎭 角色实体
│   │   │       │
│   │   │       ├── dto/                                # 📝 数据传输对象
│   │   │       │   ├── AuthResponse.java               # 🔐 认证响应
│   │   │       │   ├── LoginRequest.java               # 🔐 登录请求
│   │   │       │   ├── RegisterRequest.java            # 👤 注册请求
│   │   │       │   ├── UserDTO.java                    # 👤 用户DTO
│   │   │       │   ├── UserUpdateRequest.java          # 📝 用户更新请求
│   │   │       │   ├── PasswordChangeRequest.java      # 🔐 密码修改请求
│   │   │       │   ├── RefreshTokenRequest.java        # 🔄 刷新令牌请求
│   │   │       │   └── UserStatistics.java             # 📊 用户统计
│   │   │       │
│   │   │       ├── security/                           # 🛡️ 安全相关
│   │   │       │   ├── JwtAuthenticationFilter.java    # 🔑 JWT认证过滤器
│   │   │       │   ├── JwtAuthenticationEntryPoint.java # 🚨 JWT认证入口点
│   │   │       │   └── CustomUserDetailsService.java   # 👤 自定义用户详情服务
│   │   │       │
│   │   │       ├── exception/                          # ⚠️ 异常处理
│   │   │       │   ├── GlobalExceptionHandler.java     # 🌐 全局异常处理器
│   │   │       │   └── BusinessException.java          # 💼 业务异常
│   │   │       │
│   │   │       └── util/                               # 🔧 工具类
│   │   │           ├── JwtUtil.java                    # 🔑 JWT工具类
│   │   │           └── PasswordUtil.java               # 🔐 密码工具类
│   │   │
│   │   └── resources/
│   │       ├── application.yml                         # ⚙️ 应用配置
│   │       ├── application-dev.yml                     # 🛠️ 开发环境配置
│   │       ├── application-prod.yml                    # 🚀 生产环境配置
│   │       └── static/                                 # 📁 静态资源
│   │
│   └── test/
│       └── java/
│           └── com/cmliy/springweb/
│               ├── controller/                         # 🧪 控制器测试
│               │   ├── AuthControllerTest.java        # 🔐 认证控制器测试
│               │   └── UserControllerTest.java        # 👤 用户控制器测试
│               │
│               ├── service/                            # 🧪 服务层测试
│               │   ├── UserServiceTest.java           # 👤 用户服务测试
│               │   └── JwtAuthServiceTest.java        # 🔑 JWT服务测试
│               │
│               ├── util/                               # 🧪 工具类测试
│               │   ├── JwtUtilTest.java                # 🔑 JWT工具测试
│               │   └── PasswordUtilTest.java           # 🔐 密码工具测试
│               │
│               └── config/                             # 🧪 配置测试
│                   └── SecurityConfigTest.java        # 🛡️ 安全配置测试
│
├── frontend/                                           # 🎨 前端Vue项目
│   ├── src/
│   │   ├── views/                                     # 📄 页面组件
│   │   │   ├── Login.vue                              # 🔐 登录页面
│   │   │   └── Register.vue                           # 👤 注册页面
│   │   ├── api/                                       # 🌐 API接口
│   │   │   └── auth.js                                # 🔐 认证API
│   │   └── stores/                                    # 📦 状态管理
│   │       └── auth.js                                # 🔐 认证状态
│   └── package.json                                   # 📦 前端依赖
│
├── pom.xml                                            # 📦 Maven依赖配置
├── README.md                                          # 📖 项目说明
└── .gitignore                                         # 🚫 Git忽略文件
```

---

## 📂 详细文件说明

### 🎯 主启动类

**位置**: `src/main/java/com/cmliy/springweb/SpringWebApplication.java`

```java
package com.cmliy.springweb;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SpringWebApplication {
    public static void main(String[] args) {
        SpringApplication.run(SpringWebApplication.class, args);
    }
}
```

### 📋 配置类目录

**位置**: `src/main/java/com/cmliy/springweb/config/`

```bash
# 创建配置类目录
mkdir -p src/main/java/com/cmliy/springweb/config

# 各配置文件位置：
src/main/java/com/cmliy/springweb/config/SecurityConfig.java
src/main/java/com/cmliy/springweb/config/JwtConfig.java
src/main/java/com/cmliy/springweb/config/DatabaseConfig.java
```

### 🌐 控制器层目录

**位置**: `src/main/java/com/cmliy/springweb/controller/`

```bash
# 创建控制器目录
mkdir -p src/main/java/com/cmliy/springweb/controller

# 各控制器文件位置：
src/main/java/com/cmliy/springweb/controller/AuthController.java
src/main/java/com/cmliy/springweb/controller/UserController.java
```

### 🛠️ 服务层目录

**位置**: `src/main/java/com/cmliy/springweb/service/`

```bash
# 创建服务层目录
mkdir -p src/main/java/com/cmliy/springweb/service

# 各服务文件位置：
src/main/java/com/cmliy/springweb/service/AuthService.java
src/main/java/com/cmliy/springweb/service/AuthServiceImpl.java
src/main/java/com/cmliy/springweb/service/UserService.java
src/main/java/com/cmliy/springweb/service/UserServiceImpl.java
src/main/java/com/cmliy/springweb/service/JwtAuthService.java
```

### 📊 数据访问层目录

**位置**: `src/main/java/com/cmliy/springweb/repository/`

```bash
# 创建Repository目录
mkdir -p src/main/java/com/cmliy/springweb/repository

# 各Repository文件位置：
src/main/java/com/cmliy/springweb/repository/UserRepository.java
src/main/java/com/cmliy/springweb/repository/RoleRepository.java
```

### 🏗️ 实体模型目录

**位置**: `src/main/java/com/cmliy/springweb/model/`

```bash
# 创建模型目录
mkdir -p src/main/java/com/cmliy/springweb/model

# 各实体文件位置：
src/main/java/com/cmliy/springweb/model/User.java
src/main/java/com/cmliy/springweb/model/Role.java
```

### 📝 DTO目录

**位置**: `src/main/java/com/cmliy/springweb/dto/`

```bash
# 创建DTO目录
mkdir -p src/main/java/com/cmliy/springweb/dto

# 各DTO文件位置：
src/main/java/com/cmliy/springweb/dto/AuthResponse.java
src/main/java/com/cmliy/springweb/dto/LoginRequest.java
src/main/java/com/cmliy/springweb/dto/RegisterRequest.java
src/main/java/com/cmliy/springweb/dto/UserDTO.java
# ... 其他DTO文件
```

---

## 🛠️ 创建目录的命令

### Windows系统

```cmd
# 创建主要目录结构
mkdir src\main\java\com\cmliy\springweb\config
mkdir src\main\java\com\cmliy\springweb\controller
mkdir src\main\java\com\cmliy\springweb\service
mkdir src\main\java\com\cmliy\springweb\repository
mkdir src\main\java\com\cmliy\springweb\model
mkdir src\main\java\com\cmliy\springweb\dto
mkdir src\main\java\com\cmliy\springweb\security
mkdir src\main\java\com\cmliy\springweb\exception
mkdir src\main\java\com\cmliy\springweb\util

# 创建测试目录
mkdir src\test\java\com\cmliy\springweb\controller
mkdir src\test\java\com\cmliy\springweb\service
mkdir src\test\java\com\cmliy\springweb\util
mkdir src\test\java\com\cmliy\springweb\config
```

### Linux/Mac系统

```bash
# 创建主要目录结构
mkdir -p src/main/java/com/cmliy/springweb/config
mkdir -p src/main/java/com/cmliy/springweb/controller
mkdir -p src/main/java/com/cmliy/springweb/service
mkdir -p src/main/java/com/cmliy/springweb/repository
mkdir -p src/main/java/com/cmliy/springweb/model
mkdir -p src/main/java/com/cmliy/springweb/dto
mkdir -p src/main/java/com/cmliy/springweb/security
mkdir -p src/main/java/com/cmliy/springweb/exception
mkdir -p src/main/java/com/cmliy/springweb/util

# 创建测试目录
mkdir -p src/test/java/com/cmliy/springweb/controller
mkdir -p src/test/java/com/cmliy/springweb/service
mkdir -p src/test/java/com/cmliy/springweb/util
mkdir -p src/test/java/com/cmliy/springweb/config
```

---

## 📝 配置文件位置

### 应用配置文件

**位置**: `src/main/resources/application.yml`

```yaml
spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/clothes_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  # JWT配置
  jwt:
    secret: mySecretKey123456789012345678901234567890
    expiration: 86400000  # 24小时

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    com.cmliy.springweb: DEBUG
    org.springframework.security: DEBUG
```

### 开发环境配置

**位置**: `src/main/resources/application-dev.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clothes_db?useSSL=false&serverTimezone=UTC
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    root: INFO
    com.cmliy.springweb: DEBUG
```

### 生产环境配置

**位置**: `src/main/resources/application-prod.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://prod-server:3306/clothes_db?useSSL=true&serverTimezone=UTC
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: WARN
    com.cmliy.springweb: INFO
```

---

## 🧪 测试文件位置

### 控制器测试

**位置**: `src/test/java/com/cmliy/springweb/controller/AuthControllerTest.java`

```java
package com.cmliy.springweb.controller;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureWebMvc;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
@AutoConfigureWebMvc
public class AuthControllerTest {

    @Test
    public void testLogin() {
        // 测试登录接口
    }
}
```

### 服务层测试

**位置**: `src/test/java/com/cmliy/springweb/service/UserServiceTest.java`

```java
package com.cmliy.springweb.service;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@SpringBootTest
@ActiveProfiles("test")
public class UserServiceTest {

    @Test
    public void testRegisterUser() {
        // 测试用户注册
    }
}
```

---

## 📋 文件创建清单

### ✅ 必须创建的文件

| 文件类型 | 文件路径 | 说明 |
|---------|----------|------|
| 主启动类 | `src/main/java/com/cmliy/springweb/SpringWebApplication.java` | Spring Boot启动类 |
| 安全配置 | `src/main/java/com/cmliy/springweb/config/SecurityConfig.java` | Spring Security配置 |
| JWT配置 | `src/main/java/com/cmliy/springweb/config/JwtConfig.java` | JWT参数配置 |
| 认证控制器 | `src/main/java/com/cmliy/springweb/controller/AuthController.java` | 认证API接口 |
| 用户控制器 | `src/main/java/com/cmliy/springweb/controller/UserController.java` | 用户管理API |
| 用户实体 | `src/main/java/com/cmliy/springweb/model/User.java` | 用户数据模型 |
| 用户服务 | `src/main/java/com/cmliy/springweb/service/UserServiceImpl.java` | 用户业务逻辑 |
| 用户仓库 | `src/main/java/com/cmliy/springweb/repository/UserRepository.java` | 用户数据访问 |
| JWT工具 | `src/main/java/com/cmliy/springweb/util/JwtUtil.java` | JWT处理工具 |
| 应用配置 | `src/main/resources/application.yml` | 应用配置文件 |

### 📝 可选创建的文件

| 文件类型 | 文件路径 | 说明 |
|---------|----------|------|
| 全局异常处理 | `src/main/java/com/cmliy/springweb/exception/GlobalExceptionHandler.java` | 统一异常处理 |
| 业务异常 | `src/main/java/com/cmliy/springweb/exception/BusinessException.java` | 自定义异常 |
| JWT过滤器 | `src/main/java/com/cmliy/springweb/security/JwtAuthenticationFilter.java` | JWT认证过滤 |
| 密码工具 | `src/main/java/com/cmliy/springweb/util/PasswordUtil.java` | 密码处理工具 |
| DTO类 | `src/main/java/com/cmliy/springweb/dto/*.java` | 数据传输对象 |

---

## 🚀 快速创建脚本

### Windows批处理脚本

**文件**: `create-dirs.bat`

```cmd
@echo off
echo 创建Spring Boot项目目录结构...

REM 创建主要目录
mkdir src\main\java\com\cmliy\springweb\config
mkdir src\main\java\com\cmliy\springweb\controller
mkdir src\main\java\com\cmliy\springweb\service
mkdir src\main\java\com\cmliy\springweb\repository
mkdir src\main\java\com\cmliy\springweb\model
mkdir src\main\java\com\cmliy\springweb\dto
mkdir src\main\java\com\cmliy\springweb\security
mkdir src\main\java\com\cmliy\springweb\exception
mkdir src\main\java\com\cmliy\springweb\util

REM 创建测试目录
mkdir src\test\java\com\cmliy\springweb\controller
mkdir src\test\java\com\cmliy\springweb\service
mkdir src\test\java\com\cmliy\springweb\util

echo 目录结构创建完成！
pause
```

### Linux/Mac Shell脚本

**文件**: `create-dirs.sh`

```bash
#!/bin/bash
echo "创建Spring Boot项目目录结构..."

# 创建主要目录
mkdir -p src/main/java/com/cmliy/springweb/{config,controller,service,repository,model,dto,security,exception,util}
mkdir -p src/test/java/com/cmliy/springweb/{controller,service,util,config}

echo "目录结构创建完成！"
```

---

## 📝 本章小结

### ✅ 已明确内容

- [ ] **项目目录结构** - 完整的文件组织方式
- [ ] **文件位置说明** - 每个文件的具体路径
- [ ] **创建命令** - 不同系统的目录创建方法
- [ ] **配置文件位置** - 各种配置文件的放置位置
- [ ] **测试文件结构** - 测试代码的组织方式

### 🎯 关键要点

1. **标准Maven结构** - 遵循Maven项目约定
2. **分层架构** - Controller、Service、Repository分层
3. **配置分离** - 配置类统一管理
4. **测试对应** - 测试文件与源码结构对应

### 🚀 下一步

现在你清楚了文件结构，可以：
1. **创建目录结构** - 使用脚本或手动创建
2. **逐个创建文件** - 按照教程创建各个类文件
3. **配置应用** - 设置application.yml等配置文件
4. **运行测试** - 验证项目结构正确性

---

**记住：良好的文件组织是项目维护的基础！** 🎉

---
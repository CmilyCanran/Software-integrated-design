# 第1章：项目环境准备

> **学习目标**：搭建Vue 3开发环境，配置项目基础结构和依赖

## 🎯 本章概览

| 内容 | 预计时间 | 难度 | 状态 |
|------|----------|------|------|
| Vue项目创建 | 15分钟 | ⭐⭐ | ⏳ |
| 依赖安装配置 | 10分钟 | ⭐ | ⏳ |
| 项目结构理解 | 5分钟 | ⭐ | ⏳ |

---

## 📋 环境要求

### 必需软件
- **Node.js 16+** - JavaScript运行环境
- **npm 8+** - 包管理器（Node.js自带）
- **代码编辑器** - 推荐VS Code

### 检查环境
```bash
# 检查Node.js版本
node -v

# 检查npm版本
npm -v
```

> **提示**：如果Node.js版本低于16，请先升级到最新版本

---

## 🚀 创建Vue项目

### 第一步：使用create-vue创建项目
```bash
# 在合适的目录下创建项目
npm create vue@latest clothing-store-frontend
```

### 第二步：选择项目配置
```
✔ Project name: … clothing-store-frontend
✔ Add TypeScript? … No          # 初学者暂时跳过
✔ Add JSX Support? … No          # 不需要JSX
✔ Add Vue Router for Single Page Application development? … Yes  # 必需
✔ Add Pinia for state management? … Yes  # 必需
✔ Add Vitest for Unit Testing? … No      # 暂时跳过
✔ Add an End-to-End Testing Solution? › No  # 暂时跳过
✔ Add ESLint for code quality? … Yes     # 必需
◻ Oxlint                       # ← 选择 No
◻ rolldown-vite（试验阶段）      # ← 选择 No
```

### 第三步：进入项目并安装基础依赖
```bash
# 进入项目目录
cd clothing-store-frontend

# 安装项目基础依赖
npm install
```

---

## 📦 安装额外依赖

### UI组件库和工具
```bash
# 安装Element Plus UI组件库
npm install element-plus

# 安装Element Plus图标库
npm install @element-plus/icons-vue

# 安装HTTP请求库
npm install axios

# 安装日期处理库（后续使用）
npm install dayjs
```

### 依赖说明
| 依赖包 | 用途 | 为什么需要 |
|--------|------|------------|
| element-plus | UI组件库 | 提供美观的界面组件，避免手写CSS |
| @element-plus/icons-vue | 图标库 | 提供丰富的图标资源 |
| axios | HTTP客户端 | 用于前后端API通信 |
| dayjs | 日期处理 | 格式化日期时间显示 |

---

## 🏗️ 项目结构理解

### 创建后的项目结构
```
clothing-store-frontend/
├── public/                 # 静态资源
│   ├── favicon.ico
│   └── ...
├── src/                   # 源代码目录
│   ├── assets/           # 资源文件
│   ├── components/       # 公共组件
│   ├── views/           # 页面组件
│   ├── router/          # 路由配置
│   ├── stores/          # 状态管理
│   ├── App.vue          # 根组件
│   └── main.js          # 入口文件
├── package.json         # 项目配置
├── vite.config.js       # 构建配置
└── README.md           # 项目说明
```

### 关键文件说明
- **`src/main.js`** - 应用入口，负责初始化Vue应用
- **`src/App.vue`** - 根组件，所有页面的容器
- **`src/router/`** - 路由配置，管理页面跳转
- **`src/stores/`** - 状态管理，管理全局数据
- **`src/views/`** - 页面组件，存放各个页面

---

## 🎨 配置Element Plus

### 修改 `src/main.js`
```javascript
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import { createPinia } from 'pinia'

// 🔹 Element Plus配置
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

const app = createApp(App)

// 🔹 注册Element Plus所有图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

// 🔹 使用插件
app.use(createPinia())
app.use(router)
app.use(ElementPlus)

app.mount('#app')
```

### 配置说明
- **Element Plus** - UI组件库，提供按钮、表单等组件
- **图标注册** - 让所有Element Plus图标全局可用
- **CSS引入** - 引入Element Plus的样式文件

---

## 🧪 验证环境配置

### 启动开发服务器
```bash
# 在项目根目录下执行
npm run dev
```

### 访问项目
打开浏览器访问：`http://localhost:5173`

### 预期效果
- 看到"Welcome to Your Vue.js App"页面
- 控制台没有错误信息
- 页面样式正常显示

---

## 📝 本章小结

### ✅ 完成内容
- [x] 创建Vue 3项目
- [x] 配置项目依赖
- [x] 集成Element Plus UI库
- [x] 理解项目结构

### 🎯 掌握技能
- Vue 3项目创建流程
- npm包管理使用
- Element Plus基础配置
- 项目结构理解

### 🚀 下一步
项目环境已准备完成，下一章我们将学习Vue 3的核心概念，为开发用户认证功能打下基础。

---

## ❓ 常见问题

### Q1: npm install很慢怎么办？
**A**: 可以使用国内镜像源：
```bash
npm config set registry https://registry.npmmirror.com
```

### Q2: 项目启动失败怎么办？
**A**: 检查以下几点：
1. Node.js版本是否满足要求
2. 是否在正确的目录下执行命令
3. 网络连接是否正常

### Q3: Element Plus配置失败怎么办？
**A**: 确认以下几点：
1. 依赖是否正确安装
2. main.js代码是否正确复制
3. 重新启动开发服务器

---

**恭喜！您已经成功搭建了Vue 3开发环境。** 🎉

**下一章：[02-Vue核心概念学习](02-Vue核心概念学习.md)**
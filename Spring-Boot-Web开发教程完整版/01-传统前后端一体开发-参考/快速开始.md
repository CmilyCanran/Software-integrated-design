# 🚀 快速开始指南

## ⚡ 5分钟快速体验

如果您已经具备Spring Boot基础，可以直接按照以下步骤快速开始：

### 1. 环境检查
```bash
# 检查Java版本
java -version

# 检查Maven版本
mvn -version

# 检查MySQL服务
mysql --version
```

### 2. 快速配置

#### 更新pom.xml
```xml
<dependencies>
    <!-- Spring Boot Starters -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- 数据库驱动 -->
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <scope>runtime</scope>
    </dependency>

    <!-- 开发工具 -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>
</dependencies>
```

#### 配置application.properties
```properties
# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/springweb?useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=springweb
spring.datasource.password=springweb123

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 服务器配置
server.port=8080
```

### 3. 创建核心代码

#### User实体类
```java
// src/main/java/com/cmliy/springweb/entity/User.java
@Entity
@Table(name = "users")
@Data
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false, unique = true, length = 50)
    private String username;

    @Column(nullable = false, length = 100)
    private String email;

    @Column(nullable = false)
    private Boolean enabled = true;

    @Column(name = "created_at")
    private LocalDateTime createdAt;
}
```

#### UserRepository接口
```java
// src/main/java/com/cmliy/springweb/repository/UserRepository.java
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
    List<User> findByEmailContainingIgnoreCase(String keyword);
}
```

#### UserService类
```java
// src/main/java/com/cmliy/springweb/service/UserService.java
@Service
public class UserService {

    @Autowired
    private UserRepository userRepository;

    public List<User> getAllUsers() {
        return userRepository.findAll();
    }

    public User createUser(User user) {
        user.setCreatedAt(LocalDateTime.now());
        return userRepository.save(user);
    }

    public Optional<User> getUserById(Long id) {
        return userRepository.findById(id);
    }
}
```

#### UserController类
```java
// src/main/java/com/cmliy/springweb/controller/UserController.java
@Controller
@RequestMapping("/users")
public class UserController {

    @Autowired
    private UserService userService;

    @GetMapping
    public String listUsers(Model model) {
        model.addAttribute("users", userService.getAllUsers());
        return "user/list";
    }

    @GetMapping("/new")
    public String createUserForm(Model model) {
        model.addAttribute("user", new User());
        return "user/form";
    }

    @PostMapping
    public String createUser(@ModelAttribute User user) {
        userService.createUser(user);
        return "redirect:/users";
    }
}
```

### 4. 创建前端页面

#### 用户列表页面
```html
<!-- src/main/resources/templates/user/list.html -->
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>用户列表</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>用户列表</h1>
        <a href="/users/new" class="btn btn-primary mb-3">添加用户</a>

        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>用户名</th>
                    <th>邮箱</th>
                    <th>状态</th>
                    <th>创建时间</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="user : ${users}">
                    <td th:text="${user.id}"></td>
                    <td th:text="${user.username}"></td>
                    <td th:text="${user.email}"></td>
                    <td th:text="${user.enabled ? '启用' : '禁用'}"></td>
                    <td th:text="${user.createdAt}"></td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>
```

#### 用户表单页面
```html
<!-- src/main/resources/templates/user/form.html -->
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>用户表单</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>添加用户</h1>

        <form th:action="@{/users}" th:object="${user}" method="post">
            <div class="mb-3">
                <label class="form-label">用户名</label>
                <input type="text" class="form-control" th:field="*{username}" required>
            </div>

            <div class="mb-3">
                <label class="form-label">邮箱</label>
                <input type="email" class="form-control" th:field="*{email}" required>
            </div>

            <div class="mb-3">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" th:field="*{enabled}">
                    <label class="form-check-label">启用用户</label>
                </div>
            </div>

            <button type="submit" class="btn btn-primary">保存</button>
            <a href="/users" class="btn btn-secondary">取消</a>
        </form>
    </div>
</body>
</html>
```

### 5. 运行测试

```bash
# 启动应用
mvn spring-boot:run

# 或者直接运行主类
java -jar target/SpringWeb-0.0.1-SNAPSHOT.jar
```

访问 `http://localhost:8080/users` 查看效果！

---

## 🎯 完整学习路径

如果您是初学者，建议按照以下完整路径学习：

### 📚 详细教程顺序

1. **[如何学习](如何学习.md)** - 了解学习方法和建议
2. **[README](README.md)** - 查看完整教程概览
3. **第一阶段：[项目基础配置](01-项目基础配置/)**
   - [依赖配置](01-项目基础配置/01-依赖配置.md)
   - [数据库配置](01-项目基础配置/02-数据库配置.md)
   - [项目结构](01-项目基础配置/03-项目结构.md)
4. **第二阶段：[数据层开发](02-数据层开发/)**
   - [实体类设计](02-数据层开发/01-实体类设计.md)
   - [Repository开发](02-数据层开发/02-Repository开发.md)
   - [数据库设计](02-数据层开发/03-数据库设计.md)
5. **第三阶段：[业务层开发](03-业务层开发/)** (待完成)
6. **第四阶段：[控制层开发](04-控制层开发/)** (待完成)
7. **第五阶段：[前端页面开发](05-前端页面开发/)** (待完成)
8. **第六阶段：[高级功能](06-高级功能/)** (待完成)

### 📅 学习时间安排

| 阶段 | 内容 | 预计时间 | 难度 |
|------|------|----------|------|
| 1 | 项目基础配置 | 2-3天 | ⭐⭐ |
| 2 | 数据层开发 | 3-4天 | ⭐⭐⭐ |
| 3 | 业务层开发 | 2-3天 | ⭐⭐⭐ |
| 4 | 控制层开发 | 2-3天 | ⭐⭐ |
| 5 | 前端页面开发 | 3-4天 | ⭐⭐⭐ |
| 6 | 高级功能 | 2-3天 | ⭐⭐⭐⭐ |

**总计**: 约2-3周完成全部学习

---

## 🛠️ 开发工具推荐

### IDE配置
- **IntelliJ IDEA Ultimate** - 推荐使用
- **Visual Studio Code** - 轻量级选择
- **Eclipse** - 免费选择

### 必装插件
- **Lombok Plugin** - 简化Java代码
- **Spring Boot Helper** - Spring Boot支持
- **Database Navigator** - 数据库管理
- **RESTful Toolkit** - API开发工具

### 测试工具
- **Postman** - API测试
- **MySQL Workbench** - 数据库管理
- **Git** - 版本控制

---

## 🔧 常见问题快速解决

### 问题1：项目启动失败
```bash
# 检查Java版本
java -version
# 确保是Java 21

# 清理Maven缓存
mvn clean install

# 检查端口占用
netstat -ano | findstr :8080
```

### 问题2：数据库连接失败
```sql
-- 检查MySQL服务
SHOW DATABASES;

-- 检查用户权限
SELECT user, host FROM mysql.user;

-- 重置密码
ALTER USER 'springweb'@'localhost' IDENTIFIED BY 'springweb123';
```

### 问题3：依赖下载慢
```xml
<!-- settings.xml 添加阿里云镜像 -->
<mirror>
    <id>aliyunmaven</id>
    <mirrorOf>*</mirrorOf>
    <name>阿里云公共仓库</name>
    <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

---

## 📞 获取帮助

### 📚 官方文档
- [Spring Boot官方文档](https://docs.spring.io/spring-boot/docs/current/reference/html/)
- [Spring Data JPA文档](https://docs.spring.io/spring-data/jpa/docs/current/reference/html/)
- [Thymeleaf官方文档](https://www.thymeleaf.org/doc/tutorials/3.0/usingthymeleaf.html)

### 🌐 技术社区
- [Stack Overflow](https://stackoverflow.com/questions/tagged/spring-boot)
- [Spring中文社区](https://spring.io/)
- [CSDN](https://www.csdn.net/)

### 💬 学习交流
- 加入技术交流群
- 关注技术博客
- 参与开源项目

---

**开始您的Spring Boot Web开发之旅吧！** 🎉

记住：最好的学习方式就是动手实践。不要害怕犯错，每个错误都是学习的机会。
# 页面布局设计

## 📋 学习目标

- 掌握响应式页面布局设计
- 学会使用Bootstrap框架
- 了解CSS Grid和Flexbox布局
- 创建统一的页面主题和风格

## 🏗️ 响应式布局基础

### 1. Bootstrap网格系统

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>响应式布局示例</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* 自定义样式 */
        .demo-box {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 1rem;
            margin-bottom: 1rem;
            text-align: center;
        }
        .sidebar {
            background-color: #e9ecef;
            min-height: 100vh;
        }
    </style>
</head>
<body>
    <!-- 响应式容器 -->
    <div class="container">
        <h1>固定宽度容器</h1>
        <div class="demo-box">在不同屏幕上最大宽度不同</div>
    </div>

    <div class="container-fluid">
        <h1>流式容器</h1>
        <div class="demo-box">占满整个视口宽度</div>
    </div>

    <!-- 网格系统 -->
    <div class="container mt-4">
        <h2>网格系统示例</h2>

        <!-- 12列网格 -->
        <div class="row">
            <div class="col demo-box">列1</div>
            <div class="col demo-box">列2</div>
            <div class="col demo-box">列3</div>
        </div>

        <!-- 指定列宽 -->
        <div class="row">
            <div class="col-4 demo-box">4列宽</div>
            <div class="col-8 demo-box">8列宽</div>
        </div>

        <!-- 响应式列 -->
        <div class="row">
            <div class="col-12 col-md-6 col-lg-4 demo-box">
                小屏12列，中屏6列，大屏4列
            </div>
            <div class="col-12 col-md-6 col-lg-8 demo-box">
                小屏12列，中屏6列，大屏8列
            </div>
        </div>
    </div>
</body>
</html>
```

### 2. 管理后台布局

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head th:replace="~{fragments/header :: header}">
    <title th:text="${title}">管理后台</title>
    <style>
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            height: 100vh;
            width: 250px;
            background-color: #343a40;
            padding-top: 56px;
            z-index: 1030;
        }

        .main-content {
            margin-left: 250px;
            padding-top: 56px;
            min-height: 100vh;
            background-color: #f8f9fa;
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
            }

            .sidebar.show {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
            }
        }

        .card-stats {
            border-left: 4px solid #007bff;
        }

        .sidebar .nav-link {
            color: rgba(255, 255, 255, 0.8);
            padding: 0.75rem 1rem;
        }

        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            color: white;
            background-color: rgba(255, 255, 255, 0.1);
        }

        .content-wrapper {
            padding: 2rem;
        }
    </style>
</head>
<body>
    <!-- 顶部导航栏 -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <button class="navbar-toggler d-md-none" type="button" id="sidebarToggle">
                <span class="navbar-toggler-icon"></span>
            </button>

            <a class="navbar-brand" th:href="@{/dashboard}">
                <i class="fas fa-cogs me-2"></i>用户管理系统
            </a>

            <div class="navbar-nav ms-auto">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user-circle me-1"></i>
                        <span th:text="${#authentication.name}">管理员</span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" th:href="@{/profile}"><i class="fas fa-user me-2"></i>个人资料</a></li>
                        <li><a class="dropdown-item" th:href="@{/settings}"><i class="fas fa-cog me-2"></i>设置</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" th:href="@{/logout}"><i class="fas fa-sign-out-alt me-2"></i>退出登录</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <!-- 侧边栏 -->
    <aside class="sidebar" id="sidebar">
        <nav class="nav flex-column">
            <a class="nav-link" th:href="@{/dashboard}">
                <i class="fas fa-tachometer-alt me-2"></i>仪表板
            </a>

            <div class="nav-item">
                <a class="nav-link" th:href="@{/users}"
                   th:classappend="${#request.requestURI.startsWith('/users')} ? 'active'">
                    <i class="fas fa-users me-2"></i>用户管理
                </a>
            </div>

            <div class="nav-item">
                <a class="nav-link" th:href="@{/roles}"
                   th:classappend="${#request.requestURI.startsWith('/roles')} ? 'active'">
                    <i class="fas fa-user-shield me-2"></i>角色管理
                </a>
            </div>

            <div class="nav-item">
                <a class="nav-link" th:href="@{/permissions}"
                   th:classappend="${#request.requestURI.startsWith('/permissions')} ? 'active'">
                    <i class="fas fa-key me-2"></i>权限管理
                </a>
            </div>

            <div class="nav-item">
                <a class="nav-link" th:href="@{/statistics}"
                   th:classappend="${#request.requestURI.startsWith('/statistics')} ? 'active'">
                    <i class="fas fa-chart-bar me-2"></i>统计报表
                </a>
            </div>

            <div class="nav-item">
                <a class="nav-link" th:href="@{/logs}"
                   th:classappend="${#request.requestURI.startsWith('/logs')} ? 'active'">
                    <i class="fas fa-file-alt me-2"></i>系统日志
                </a>
            </div>

            <div class="nav-item">
                <a class="nav-link" th:href="@{/settings}"
                   th:classappend="${#request.requestURI.startsWith('/settings')} ? 'active'">
                    <i class="fas fa-cog me-2"></i>系统设置
                </a>
            </div>
        </nav>
    </aside>

    <!-- 主要内容区域 -->
    <main class="main-content">
        <div layout:fragment="content">
            <!-- 默认内容 -->
        </div>
    </main>

    <!-- JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.0.0/js/all.min.js"></script>
    <script>
        // 侧边栏切换（移动端）
        document.getElementById('sidebarToggle').addEventListener('click', function() {
            document.getElementById('sidebar').classList.toggle('show');
        });

        // 点击外部关闭侧边栏（移动端）
        document.addEventListener('click', function(e) {
            const sidebar = document.getElementById('sidebar');
            const toggle = document.getElementById('sidebarToggle');

            if (window.innerWidth <= 768 &&
                !sidebar.contains(e.target) &&
                !toggle.contains(e.target) &&
                sidebar.classList.contains('show')) {
                sidebar.classList.remove('show');
            }
        });
    </script>
</body>
</html>
```

### 3. 仪表板页面

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layout/admin}">

<head>
    <title>仪表板 - 用户管理系统</title>
</head>

<body>
<div layout:fragment="content">
    <div class="content-wrapper">
        <!-- 页面标题 -->
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
            <h1 class="h2">仪表板</h1>
            <div class="btn-toolbar mb-2 mb-md-0">
                <div class="btn-group me-2">
                    <button type="button" class="btn btn-sm btn-outline-secondary">
                        <i class="fas fa-download me-1"></i>导出
                    </button>
                </div>
                <button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle">
                    <i class="fas fa-calendar me-1"></i>
                    <span th:text="${#temporals.format(#temporals.createNow(), 'yyyy-MM')}">2024-01</span>
                </button>
            </div>
        </div>

        <!-- 统计卡片 -->
        <div class="row mb-4">
            <!-- 用户总数 -->
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-left-primary shadow h-100 py-2 card-stats">
                    <div class="card-body">
                        <div class="row no-gutters align-items-center">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                    用户总数
                                </div>
                                <div class="h5 mb-0 font-weight-bold text-gray-800" th:text="${stats.totalUsers}">
                                    1,234
                                </div>
                            </div>
                            <div class="col-auto">
                                <i class="fas fa-users fa-2x text-gray-300"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 活跃用户 -->
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-left-success shadow h-100 py-2 card-stats">
                    <div class="card-body">
                        <div class="row no-gutters align-items-center">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                                    活跃用户
                                </div>
                                <div class="h5 mb-0 font-weight-bold text-gray-800" th:text="${stats.activeUsers}">
                                    856
                                </div>
                            </div>
                            <div class="col-auto">
                                <i class="fas fa-user-check fa-2x text-gray-300"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 今日新增 -->
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-left-info shadow h-100 py-2 card-stats">
                    <div class="card-body">
                        <div class="row no-gutters align-items-center">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                                    今日新增
                                </div>
                                <div class="h5 mb-0 font-weight-bold text-gray-800" th:text="${stats.todayNewUsers}">
                                    23
                                </div>
                            </div>
                            <div class="col-auto">
                                <i class="fas fa-user-plus fa-2x text-gray-300"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 系统负载 -->
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-left-warning shadow h-100 py-2 card-stats">
                    <div class="card-body">
                        <div class="row no-gutters align-items-center">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                                    系统负载
                                </div>
                                <div class="h5 mb-0 font-weight-bold text-gray-800" th:text="${stats.systemLoad + '%'}">
                                    65%
                                </div>
                            </div>
                            <div class="col-auto">
                                <i class="fas fa-server fa-2x text-gray-300"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 图表区域 -->
        <div class="row">
            <!-- 用户增长图表 -->
            <div class="col-xl-8 col-lg-7">
                <div class="card shadow mb-4">
                    <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                        <h6 class="m-0 font-weight-bold text-primary">用户增长趋势</h6>
                        <div class="dropdown no-arrow">
                            <a class="dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                            </a>
                            <div class="dropdown-menu dropdown-menu-right shadow">
                                <a class="dropdown-item" href="#">最近7天</a>
                                <a class="dropdown-item" href="#">最近30天</a>
                                <a class="dropdown-item" href="#">最近90天</a>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="chart-area">
                            <canvas id="userGrowthChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 用户分布 -->
            <div class="col-xl-4 col-lg-5">
                <div class="card shadow mb-4">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">用户角色分布</h6>
                    </div>
                    <div class="card-body">
                        <div class="chart-pie pt-4">
                            <canvas id="userRoleChart"></canvas>
                        </div>
                        <div class="mt-4 text-center small">
                            <span class="mr-2">
                                <i class="fas fa-circle text-primary"></i> 管理员
                            </span>
                            <span class="mr-2">
                                <i class="fas fa-circle text-success"></i> 普通用户
                            </span>
                            <span class="mr-2">
                                <i class="fas fa-circle text-info"></i> VIP用户
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 最近活动 -->
        <div class="row">
            <div class="col-lg-6 mb-4">
                <div class="card shadow">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">最近用户活动</h6>
                    </div>
                    <div class="card-body">
                        <div class="list-group list-group-flush">
                            <div class="list-group-item" th:each="activity : ${recentActivities}">
                                <div class="d-flex w-100 justify-content-between">
                                    <h6 class="mb-1" th:text="${activity.username}">用户名</h6>
                                    <small th:text="${#temporals.format(activity.timestamp, 'HH:mm')}">12:30</small>
                                </div>
                                <p class="mb-1" th:text="${activity.description}">活动描述</p>
                                <small th:text="${activity.ip}">IP地址</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 系统状态 -->
            <div class="col-lg-6 mb-4">
                <div class="card shadow">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">系统状态</h6>
                    </div>
                    <div class="card-body">
                        <div class="row no-gutters align-items-center mb-3">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-success mb-1">CPU使用率</div>
                                <div class="progress">
                                    <div class="progress-bar bg-success" role="progressbar"
                                         th:style="'width: ' + ${systemStatus.cpuUsage} + '%'"
                                         th:text="${systemStatus.cpuUsage + '%'}">45%</div>
                                </div>
                            </div>
                        </div>

                        <div class="row no-gutters align-items-center mb-3">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-info mb-1">内存使用率</div>
                                <div class="progress">
                                    <div class="progress-bar bg-info" role="progressbar"
                                         th:style="'width: ' + ${systemStatus.memoryUsage} + '%'"
                                         th:text="${systemStatus.memoryUsage + '%'}">62%</div>
                                </div>
                            </div>
                        </div>

                        <div class="row no-gutters align-items-center mb-3">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-warning mb-1">磁盘使用率</div>
                                <div class="progress">
                                    <div class="progress-bar bg-warning" role="progressbar"
                                         th:style="'width: ' + ${systemStatus.diskUsage} + '%'"
                                         th:text="${systemStatus.diskUsage + '%'}">78%</div>
                                </div>
                            </div>
                        </div>

                        <div class="row no-gutters align-items-center">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold mb-1">网络状态</div>
                                <span class="badge bg-success">正常</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<th:block layout:fragment="scripts">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // 用户增长图表
        const userGrowthCtx = document.getElementById('userGrowthChart').getContext('2d');
        const userGrowthChart = new Chart(userGrowthCtx, {
            type: 'line',
            data: {
                labels: ['1月', '2月', '3月', '4月', '5月', '6月', '7月'],
                datasets: [{
                    label: '新增用户',
                    data: [65, 59, 80, 81, 56, 55, 40],
                    borderColor: 'rgb(75, 192, 192)',
                    backgroundColor: 'rgba(75, 192, 192, 0.2)',
                    tension: 0.1
                }]
            },
            options: {
                responsive: true,
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });

        // 用户角色分布图表
        const userRoleCtx = document.getElementById('userRoleChart').getContext('2d');
        const userRoleChart = new Chart(userRoleCtx, {
            type: 'doughnut',
            data: {
                labels: ['管理员', '普通用户', 'VIP用户'],
                datasets: [{
                    data: [12, 856, 366],
                    backgroundColor: [
                        'rgb(255, 99, 132)',
                        'rgb(54, 162, 235)',
                        'rgb(255, 205, 86)'
                    ]
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false
            }
        });
    </script>
</th:block>
</body>
</html>
```

## 🎨 响应式表格设计

### 1. 用户列表表格

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>响应式用户表格</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .table-responsive-stack {
            display: block;
        }

        @media (max-width: 768px) {
            .table-responsive-stack thead {
                display: none;
            }

            .table-responsive-stack tbody,
            .table-responsive-stack tr,
            .table-responsive-stack td {
                display: block;
                width: 100% !important;
            }

            .table-responsive-stack tr {
                border: 1px solid #ccc;
                margin-bottom: 10px;
                padding: 10px;
            }

            .table-responsive-stack td {
                border: none;
                position: relative;
                padding-left: 50% !important;
                text-align: left;
            }

            .table-responsive-stack td:before {
                content: attr(data-label) ": ";
                position: absolute;
                left: 6px;
                width: 45%;
                font-weight: bold;
            }
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            object-fit: cover;
        }

        .status-badge {
            font-size: 0.75rem;
        }
    </style>
</head>
<body>
    <div class="container-fluid mt-4">
        <!-- 表格工具栏 -->
        <div class="row mb-3">
            <div class="col-md-6">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="搜索用户..." th:value="${keyword}">
                    <button class="btn btn-outline-secondary" type="button">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </div>
            <div class="col-md-6 text-end">
                <div class="btn-group" role="group">
                    <button class="btn btn-outline-primary btn-sm">
                        <i class="fas fa-download me-1"></i>导出
                    </button>
                    <button class="btn btn-outline-success btn-sm">
                        <i class="fas fa-plus me-1"></i>添加用户
                    </button>
                </div>
            </div>
        </div>

        <!-- 响应式表格 -->
        <div class="table-responsive">
            <table class="table table-hover table-responsive-stack">
                <thead class="table-light">
                    <tr>
                        <th scope="col">
                            <input type="checkbox" class="form-check-input">
                        </th>
                        <th scope="col">用户</th>
                        <th scope="col">邮箱</th>
                        <th scope="col">角色</th>
                        <th scope="col">状态</th>
                        <th scope="col">注册时间</th>
                        <th scope="col">操作</th>
                    </tr>
                </thead>
                <tbody>
                    <tr th:each="user : ${users.content}">
                        <td data-label="选择">
                            <input type="checkbox" class="form-check-input" th:value="${user.id}">
                        </td>
                        <td data-label="用户">
                            <div class="d-flex align-items-center">
                                <img th:src="${user.avatarUrl ?: '/images/default-avatar.png'}"
                                     class="user-avatar me-2" alt="头像">
                                <div>
                                    <div class="fw-bold" th:text="${user.username}">用户名</div>
                                    <small class="text-muted" th:text="${user.fullName}">全名</small>
                                </div>
                            </div>
                        </td>
                        <td data-label="邮箱">
                            <span th:text="${user.email}">email@example.com</span>
                        </td>
                        <td data-label="角色">
                            <span class="badge status-badge"
                                  th:classappend="${user.role.name() == 'ADMIN' ? 'bg-danger' :
                                                 user.role.name() == 'VIP' ? 'bg-warning' : 'bg-primary'}"
                                  th:text="${user.role.displayName}">角色</span>
                        </td>
                        <td data-label="状态">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox"
                                       th:checked="${user.enabled}"
                                       th:id="'status-' + ${user.id}">
                                <label class="form-check-label" th:for="'status-' + ${user.id}">
                                    <span th:text="${user.enabled} ? '启用' : '禁用'">状态</span>
                                </label>
                            </div>
                        </td>
                        <td data-label="注册时间">
                            <span th:text="${#temporals.format(user.createdAt, 'yyyy-MM-dd')}">2024-01-01</span>
                        </td>
                        <td data-label="操作">
                            <div class="btn-group btn-group-sm" role="group">
                                <button type="button" class="btn btn-outline-primary">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button type="button" class="btn btn-outline-warning">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button type="button" class="btn btn-outline-danger">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- 分页 -->
        <nav th:if="${users.totalPages > 1}">
            <ul class="pagination justify-content-center">
                <li class="page-item" th:class="${users.first} ? 'disabled'">
                    <a class="page-link" th:href="@{/users(page=0)}">
                        <i class="fas fa-angle-double-left"></i>
                    </a>
                </li>
                <li class="page-item" th:class="${users.first} ? 'disabled'">
                    <a class="page-link" th:href="@{/users(page=${users.number - 1})}">
                        <i class="fas fa-angle-left"></i>
                    </a>
                </li>

                <!-- 页码 -->
                <li th:each="pageNum : ${#numbers.sequence(0, users.totalPages - 1)}"
                    class="page-item" th:class="${pageNum == users.number} ? 'active'">
                    <a class="page-link" th:href="@{/users(page=${pageNum})}" th:text="${pageNum + 1}">1</a>
                </li>

                <li class="page-item" th:class="${users.last} ? 'disabled'">
                    <a class="page-link" th:href="@{/users(page=${users.number + 1})}">
                        <i class="fas fa-angle-right"></i>
                    </a>
                </li>
                <li class="page-item" th:class="${users.last} ? 'disabled'">
                    <a class="page-link" th:href="@{/users(page=${users.totalPages - 1})}">
                        <i class="fas fa-angle-double-right"></i>
                    </a>
                </li>
            </ul>
        </nav>
    </div>
</body>
</html>
```

## 📱 移动端优化

### 1. 移动端导航

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>移动端优化示例</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .mobile-menu {
            position: fixed;
            top: 0;
            left: -100%;
            width: 280px;
            height: 100vh;
            background-color: #343a40;
            transition: left 0.3s ease;
            z-index: 1050;
        }

        .mobile-menu.show {
            left: 0;
        }

        .mobile-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            display: none;
            z-index: 1040;
        }

        .mobile-overlay.show {
            display: block;
        }

        @media (min-width: 768px) {
            .mobile-menu {
                position: static;
                left: 0;
                width: auto;
                height: auto;
            }

            .mobile-overlay {
                display: none !important;
            }
        }

        .card-mobile {
            margin-bottom: 1rem;
            border-radius: 0.75rem;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        @media (max-width: 576px) {
            .action-buttons {
                flex-direction: column;
            }

            .action-buttons .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- 移动端菜单 -->
    <div class="mobile-menu" id="mobileMenu">
        <div class="p-3 text-white border-bottom">
            <h5 class="mb-0">菜单</h5>
        </div>
        <nav class="nav flex-column p-3">
            <a class="nav-link text-white" th:href="@{/dashboard}">
                <i class="fas fa-tachometer-alt me-2"></i>仪表板
            </a>
            <a class="nav-link text-white" th:href="@{/users}">
                <i class="fas fa-users me-2"></i>用户管理
            </a>
            <a class="nav-link text-white" th:href="@{/roles}">
                <i class="fas fa-user-shield me-2"></i>角色管理
            </a>
        </nav>
    </div>

    <!-- 遮罩层 -->
    <div class="mobile-overlay" id="mobileOverlay"></div>

    <!-- 顶部导航 -->
    <nav class="navbar navbar-dark bg-dark">
        <div class="container-fluid">
            <button class="navbar-toggler d-md-none" type="button" id="menuToggle">
                <span class="navbar-toggler-icon"></span>
            </button>
            <span class="navbar-brand mb-0 h1 mx-auto">用户管理</span>
            <button class="navbar-toggler" type="button">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </nav>

    <!-- 主要内容 -->
    <div class="container-fluid mt-3">
        <!-- 搜索栏 -->
        <div class="row mb-3">
            <div class="col-12">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="搜索用户...">
                    <button class="btn btn-primary" type="button">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </div>
        </div>

        <!-- 用户卡片（移动端） -->
        <div class="d-md-none">
            <div class="card card-mobile" th:each="user : ${users.content}">
                <div class="card-body">
                    <div class="d-flex align-items-center mb-3">
                        <img th:src="${user.avatarUrl ?: '/images/default-avatar.png'}"
                             class="rounded-circle me-3" width="48" height="48" alt="头像">
                        <div class="flex-grow-1">
                            <h5 class="card-title mb-1" th:text="${user.username}">用户名</h5>
                            <p class="card-text text-muted mb-0" th:text="${user.email}">邮箱</p>
                        </div>
                        <div class="dropdown">
                            <button class="btn btn-sm btn-outline-secondary" type="button" data-bs-toggle="dropdown">
                                <i class="fas fa-ellipsis-v"></i>
                            </button>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="#">查看</a></li>
                                <li><a class="dropdown-item" href="#">编辑</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="#">删除</a></li>
                            </ul>
                        </div>
                    </div>

                    <div class="row mb-2">
                        <div class="col-6">
                            <small class="text-muted">角色</small>
                            <div>
                                <span class="badge bg-primary" th:text="${user.role.displayName}">角色</span>
                            </div>
                        </div>
                        <div class="col-6">
                            <small class="text-muted">状态</small>
                            <div>
                                <span class="badge"
                                      th:class="${user.enabled} ? 'bg-success' : 'bg-secondary'"
                                      th:text="${user.enabled} ? '启用' : '禁用'">状态</span>
                            </div>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-12">
                            <small class="text-muted">注册时间</small>
                            <div th:text="${#temporals.format(user.createdAt, 'yyyy-MM-dd')}">2024-01-01</div>
                        </div>
                    </div>

                    <div class="action-buttons">
                        <button class="btn btn-sm btn-outline-primary flex-fill">
                            <i class="fas fa-eye me-1"></i>查看
                        </button>
                        <button class="btn btn-sm btn-outline-warning flex-fill">
                            <i class="fas fa-edit me-1"></i>编辑
                        </button>
                        <button class="btn btn-sm btn-outline-danger flex-fill">
                            <i class="fas fa-trash me-1"></i>删除
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- 桌面端表格 -->
        <div class="d-none d-md-block">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>用户</th>
                        <th>邮箱</th>
                        <th>角色</th>
                        <th>状态</th>
                        <th>注册时间</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    <tr th:each="user : ${users.content}">
                        <td th:text="${user.username}">用户名</td>
                        <td th:text="${user.email}">邮箱</td>
                        <td th:text="${user.role.displayName}">角色</td>
                        <td th:text="${user.enabled} ? '启用' : '禁用'">状态</td>
                        <td th:text="${#temporals.format(user.createdAt, 'yyyy-MM-dd')}">2024-01-01</td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <button class="btn btn-outline-primary">查看</button>
                                <button class="btn btn-outline-warning">编辑</button>
                                <button class="btn btn-outline-danger">删除</button>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <script>
        // 移动端菜单切换
        document.getElementById('menuToggle').addEventListener('click', function() {
            document.getElementById('mobileMenu').classList.add('show');
            document.getElementById('mobileOverlay').classList.add('show');
        });

        // 点击遮罩层关闭菜单
        document.getElementById('mobileOverlay').addEventListener('click', function() {
            document.getElementById('mobileMenu').classList.remove('show');
            document.getElementById('mobileOverlay').classList.remove('show');
        });
    </script>
</body>
</html>
```

## ✅ 检查点

完成本节学习后，您应该能够：

- [ ] 设计响应式页面布局
- [ ] 使用Bootstrap网格系统
- [ ] 创建管理后台布局
- [ ] 优化移动端显示效果
- [ ] 实现响应式表格设计

## 🚀 下一步

页面布局设计完成后，接下来我们将学习：
[交互功能实现](03-交互功能.md)

---

**提示**: 响应式设计是现代Web应用的基础，确保应用在各种设备上都能提供良好的用户体验。
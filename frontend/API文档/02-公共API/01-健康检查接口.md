---
tags:
  - API文档
  - 公共
  - 健康检查
created: 2025-11-21
modified: 2025-11-21
category: 公共API
difficulty: beginner
---

# 💊 健康检查接口

## 📋 接口概述

应用健康检查，用于监控系统运行状态。

## 📍 请求信息

- **HTTP方法**: `GET`
- **URL路径**: `/api/public/health`
- **权限要求**: 无需认证

## 📥 请求参数

无请求参数。

## ✅ 成功响应

### 响应结构

| 字段名 | 类型 | 说明 |
|--------|------|------|
| success | boolean | 操作是否成功 |
| code | number | 状态码 |
| message | string | 响应消息 |
| data | object | 健康检查信息 |
| data.status | string | 应用状态 |
| data.timestamp | string | 检查时间戳 |
| data.application | string | 应用名称 |
| data.version | string | 应用版本 |

### 响应示例

```json
{
  "success": true,
  "code": 200,
  "message": "服务正常运行",
  "data": {
    "status": "UP",
    "timestamp": "2025-11-21T10:30:45.123",
    "application": "SpringWeb",
    "version": "1.0.0"
  }
}
```

## 🧪 前端调用示例

```javascript
import { api } from '@/api/request'

// 健康检查函数
async function healthCheck(): Promise<void> {
  try {
    const response = await api.get('/api/public/health')
    console.log('服务状态:', response)
    if (response.status === 'UP') {
      console.log('服务正常运行')
    } else {
      console.warn('服务状态异常:', response)
    }
  } catch (error) {
    console.error('健康检查失败:', error)
  }
}

// 使用示例
healthCheck()
```
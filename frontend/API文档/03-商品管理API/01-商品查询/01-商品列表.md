---
tags:
  - API文档
  - 商品
  - 查询
created: 2025-11-21
modified: 2025-11-21
category: 商品API
difficulty: intermediate
---

# 📋 商品列表接口

## 📋 接口概述

获取商品列表，支持分页和筛选。

## 📍 请求信息

- **HTTP方法**: `GET`
- **URL路径**: `/products`
- **权限要求**: 无需认证

## 📥 请求参数

### 查询参数 (Query Parameters)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | number | 否 | 页码(从0开始) |
| size | number | 否 | 每页大小 |
| sort | string | 否 | 排序字段 |
| order | string | 否 | 排序方向(asc/desc) |
| isAvailable | boolean | 否 | 是否上架 |
| category | string | 否 | 商品分类 |
| minPrice | number | 否 | 最低价格 |
| maxPrice | number | 否 | 最高价格 |

## 📤 请求示例

```
GET /products?page=0&size=10&sort=createdAt&order=desc&isAvailable=true
```

## ✅ 成功响应

### 响应结构

| 字段名 | 类型 | 说明 |
|--------|------|------|
| success | boolean | 操作是否成功 |
| code | number | 状态码 |
| message | string | 响应消息 |
| data | object | 分页商品数据 |
| data.content | array | 商品列表 |
| data.totalElements | number | 总商品数 |
| data.totalPages | number | 总页数 |
| data.size | number | 每页大小 |
| data.number | number | 当前页码 |
| data.first | boolean | 是否第一页 |
| data.last | boolean | 是否最后一页 |

### 商品对象结构

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | number | 商品ID |
| productName | string | 商品名称 |
| description | string | 商品描述 |
| price | number | 商品价格 |
| salesCount | number | 销售数量 |
| discount | number | 折扣率 |
| stockQuantity | number | 库存数量 |
| isAvailable | boolean | 是否上架 |
| creatorId | number | 创建者ID |
| productData | object | 商品扩展数据 |
| createdAt | string | 创建时间 |
| updatedAt | string | 更新时间 |

### 响应示例

```json
{
  "success": true,
  "code": 200,
  "message": "获取商品列表成功",
  "data": {
    "content": [
      {
        "id": 1,
        "productName": "时尚连衣裙",
        "description": "春夏新款连衣裙",
        "price": 299.99,
        "salesCount": 50,
        "discount": 10.00,
        "stockQuantity": 100,
        "isAvailable": true,
        "creatorId": 1,
        "productData": {
          "image_data": {
            "main_image": "http://localhost:8080/images/main1.jpg",
            "gallery": ["http://localhost:8080/images/gallery1.jpg"],
            "thumbnails": {"small": "http://localhost:8080/images/thumb1.jpg"}
          },
          "specifications": {
            "category": "女装",
            "brand": "时尚品牌",
            "color": "红色",
            "size": "M"
          },
          "tags": ["新品", "热销"],
          "extended_attributes": {}
        },
        "createdAt": "2025-11-20T09:02:15.123",
        "updatedAt": "2025-11-21T10:30:45.123"
      }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "size": 10,
    "number": 0,
    "first": true,
    "last": true
  }
}
```

## 🧪 前端调用示例

```javascript
import { api } from '@/api/request'
import type { Product, PaginatedResponse } from '@/types/product'

// 获取商品列表函数
async function getProducts(params: {
  page?: number
  size?: number
  sort?: string
  order?: 'asc' | 'desc'
  isAvailable?: boolean
  category?: string
  minPrice?: number
  maxPrice?: number
}): Promise<PaginatedResponse<Product>> {
  try {
    const response = await api.get<PaginatedResponse<Product>>('/products', { params })
    return response
  } catch (error) {
    console.error('获取商品列表失败:', error)
    throw error
  }
}

// 使用示例
const queryParams = {
  page: 0,
  size: 10,
  sort: 'createdAt',
  order: 'desc',
  isAvailable: true
}

getProducts(queryParams)
  .then(response => {
    console.log('商品列表:', response)
    // 更新商品列表状态
    productStore.setProducts(response.data)
  })
  .catch(error => {
    console.error('获取商品列表失败:', error)
  })
```
// ============================================================================
// Vue应用入口文件 - 负责初始化和配置整个Vue应用
// ============================================================================

// 🔥 核心依赖导入：Vue 3生态系统
import { createApp } from 'vue'                    // 导入Vue应用创建函数，用于创建Vue实例
import { createPinia } from 'pinia'                // 导入Pinia状态管理库，替代Vuex
import ElementPlus from 'element-plus'             // 导入Element Plus UI组件库
import 'element-plus/dist/index.css'               // 导入Element Plus全局样式文件
import * as ElementPlusIconsVue from '@element-plus/icons-vue'  // 导入所有Element Plus图标组件

// 🔥 应用文件导入：根组件和路由配置
import App from './App.vue'                        // 导入根组件，应用的顶层容器
import router from './router'                      // 导入路由配置，管理页面导航

// ============================================================================
// Vue应用实例创建
// ============================================================================
const app = createApp(App)                        // 创建Vue应用实例，App作为根组件

// ============================================================================
// 全局组件注册：Element Plus图标
// ============================================================================
// 🔥 关键配置：将所有Element Plus图标注册为全局组件
// 这样可以在任何组件中直接使用 <el-icon><User /></el-icon> 而无需单独导入
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)                   // 注册图标组件，key为组件名，component为组件对象
}

// ============================================================================
// 插件安装：按正确顺序安装Vue插件
// ============================================================================
// 🔥 关键配置：安装核心插件到Vue应用实例
app.use(createPinia())                            // 安装Pinia状态管理，提供全局状态管理能力
app.use(router)                                   // 安装Vue Router，提供单页面应用路由功能
app.use(ElementPlus)                              // 安装Element Plus UI组件库，提供丰富的UI组件

// ============================================================================
// 应用挂载：将Vue应用挂载到DOM
// ============================================================================
app.mount('#app')                                 // 将Vue应用挂载到index.html中的#app元素上，启动应用

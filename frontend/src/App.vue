<!-- ============================================================================ -->
<!-- App.vue - Vue应用根组件 -->
<!-- ============================================================================ -->

<template>
  <!-- 🔥 路由视图容器：显示当前路由对应的页面组件 -->
  <router-view />
</template>

<!-- ============================================================================ -->
<!-- 脚本部分：使用Composition API模式 -->
<!-- ============================================================================ -->
<script setup lang="ts">
// 📝 根组件说明：
// App.vue是Vue应用的顶层组件，主要作用是作为路由容器
// 所有页面组件都会通过<router-view>渲染在这里
// 这里不需要额外的业务逻辑，保持简洁即可
</script>

<!-- ============================================================================ -->
<!-- 全局样式：定义应用的基础样式和通用类 -->
<!-- ============================================================================ -->
<style>
/* 🔥 全局样式重置：消除浏览器默认样式差异 */
* {
  margin: 0;                                              /* 清除外边距 */
  padding: 0;                                             /* 清除内边距 */
  box-sizing: border-box;                                /* 使用边框盒模型，统一计算方式 */
}

/* 🔥 页面主体样式：设置基础字体和布局 */
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;  /* 系统字体栈，优先使用系统原生字体 */
  background-color: #f5f7fa;                             /* 浅灰色背景，提供舒适的视觉体验 */
  line-height: 1.6;                                      /* 行高设置，提升文本可读性 */
  color: #333;                                            /* 深灰色文字，保证良好的对比度 */
}

/* 🔥 应用根容器：确保应用占满整个视口 */
#app {
  min-height: 100vh;                                       /* 最小高度为视口高度，确保内容填满屏幕 */
}

/* 🔥 链接样式重置：统一链接外观 */
a {
  text-decoration: none;                                  /* 去除下划线 */
  color: inherit;                                         /* 继承父元素颜色 */
}

/* 🔥 Element Plus卡片组件样式统一 */
.el-card {
  border-radius: 8px;                                     /* 圆角边框，现代化外观 */
  box-shadow: 0 2px 12px 0 rgba(0, 0, 0, 0.1);           /* 阴影效果，增加层次感 */
  border: none;                                           /* 去除默认边框，使用阴影代替 */
}

/* 🔥 页面容器通用类：用于页面内容布局 */
.page-container {
  padding: 20px;                                          /* 内边距，提供内容呼吸空间 */
  max-width: 1200px;                                      /* 最大宽度，避免在大屏幕上过宽 */
  margin: 0 auto;                                         /* 水平居中 */
}

/* 🔥 页面标题通用样式 */
.page-title {
  font-size: 24px;                                        /* 标题字体大小 */
  font-weight: 600;                                       /* 字体粗细，半粗体 */
  margin-bottom: 20px;                                    /* 下边距，与内容分隔 */
  color: #303133;                                         /* 深色文字，Element Plus标准色彩 */
}

/* 🔥 响应式设计：适配移动设备 */
@media (max-width: 768px) {                               /* 平板和手机端样式 */
  .page-container {
    padding: 10px;                                        /* 移动端减少内边距，节省空间 */
  }
}
</style>

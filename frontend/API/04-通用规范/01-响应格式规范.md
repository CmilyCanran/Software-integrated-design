---
tags:
  - API文档
  - 规范
  - 响应格式
created: 2025-11-21
modified: 2025-11-21
category: 通用规范
difficulty: beginner
---

# 📋 统一响应格式规范

## 📋 概述

所有API接口都遵循统一的响应格式，确保前端能够一致地处理响应数据。

## ✅ 成功响应格式

### 基本结构

```json
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| success | boolean | 操作是否成功 |
| code | number | 业务状态码 |
| message | string | 响应消息 |
| data | any | 实际响应数据 |

### 成功响应示例

```json
{
  "success": true,
  "code": 200,
  "message": "获取用户信息成功",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com"
  }
}
```

## ❌ 错误响应格式

### 基本结构

```json
{
  "success": false,
  "code": 400,
  "message": "错误描述",
  "data": null
}
```

### 字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| success | boolean | 操作是否成功 |
| code | number | 错误状态码 |
| message | string | 错误消息 |
| data | null | 错误数据（通常为null） |

### 错误响应示例

```json
{
  "success": false,
  "code": 401,
  "message": "用户名或密码错误",
  "data": null
}
```

## 📊 分页响应格式

### 基本结构

```json
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": {
    "content": [],
    "totalElements": 100,
    "totalPages": 10,
    "size": 10,
    "number": 0,
    "first": true,
    "last": false
  }
}
```

### 字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| content | array | 当前页数据 |
| totalElements | number | 总元素数 |
| totalPages | number | 总页数 |
| size | number | 每页大小 |
| number | number | 当前页码 |
| first | boolean | 是否第一页 |
| last | boolean | 是否最后一页 |

## 🔢 状态码规范

### 业务状态码

| 状态码 | 说明 |
|--------|------|
| 200 | 操作成功 |
| 400 | 请求参数错误 |
| 401 | 未认证 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### HTTP状态码映射

| HTTP状态码 | 业务状态码 | 说明 |
|------------|------------|------|
| 200 | 200 | 请求成功 |
| 400 | 400 | 客户端错误 |
| 401 | 401 | 未认证 |
| 403 | 403 | 权限不足 |
| 404 | 404 | 资源不存在 |
| 500 | 500 | 服务器错误 |

## 🧪 前端处理示例

```javascript
// 统一响应处理
function handleResponse(response) {
  if (response.success) {
    return response.data
  } else {
    throw new Error(response.message)
  }
}

// 使用示例
api.get('/users')
  .then(handleResponse)
  .then(data => {
    console.log('用户数据:', data)
  })
  .catch(error => {
    console.error('操作失败:', error.message)
  })
```